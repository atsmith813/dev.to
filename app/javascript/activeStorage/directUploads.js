// Scripts related to direct uploads via ActiveStorage
// There are a number of JavaScript events available. See more at:
// https://edgeguides.rubyonrails.org/active_storage_overview.html#direct-upload-javascript-events

// addEventListener("direct-upload:initialize", event => {
// })
//
// addEventListener("direct-upload:start", event => {
// })
//
// addEventListener("direct-upload:progress", event => {
// })
//
// addEventListener("direct-upload:error", event => {
// })
//
// addEventListener("direct-upload:end", event => {
// })
